# Write a program that accepts a string as input, capitalizes the first letter of each word in the
# string, and then returns the result string.

My_text = str(input("Enter a string: "))
print(My_text.title())
